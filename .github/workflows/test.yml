name: test

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  test:
    name: pytest (${{ matrix.os }} / py${{ matrix.python-version }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ['3.10', '3.11', '3.12']
        # Windows is partial-coverage: ortus init/check/tail/human work;
        # grind needs bwrap/sandbox-exec so its sandbox precheck pytest.skips.
        # Bash-shim integration tests pytest.skip on Windows.
        include:
          - os: windows-latest
            continue-on-error: true
    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v3
        with:
          enable-cache: true
          cache-dependency-glob: pyproject.toml

      - name: Set up Python ${{ matrix.python-version }}
        run: uv python install ${{ matrix.python-version }}

      - name: Install bd (beads) — Linux/macOS
        if: runner.os != 'Windows'
        run: |
          # Pinned per project convention. The release tag is `v1.0.0` but the
          # asset filenames embed the bare version `1.0.0` (no `v`).
          BD_VERSION="1.0.0"
          case "${{ runner.os }}" in
            Linux)
              ASSET="beads_${BD_VERSION}_linux_amd64.tar.gz"
              ;;
            macOS)
              if [ "$(uname -m)" = "arm64" ]; then
                ASSET="beads_${BD_VERSION}_darwin_arm64.tar.gz"
              else
                ASSET="beads_${BD_VERSION}_darwin_amd64.tar.gz"
              fi
              ;;
          esac
          URL="https://github.com/gastownhall/beads/releases/download/v${BD_VERSION}/${ASSET}"
          echo "Downloading $URL"
          # --fail so a 404 exits non-zero instead of silently writing an HTML
          # error page that tar later chokes on with a cryptic message.
          curl --fail -L -o /tmp/bd.tar.gz "$URL"
          sudo tar -xzf /tmp/bd.tar.gz -C /usr/local/bin
          bd --version

      - name: Install bd (beads) — Windows
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $BD_VERSION = "1.0.0"
          $ASSET = "beads_${BD_VERSION}_windows_amd64.zip"
          $URL = "https://github.com/gastownhall/beads/releases/download/v${BD_VERSION}/${ASSET}"
          Write-Host "Downloading $URL"
          Invoke-WebRequest -Uri $URL -OutFile bd.zip
          Expand-Archive bd.zip -DestinationPath C:\bd
          Add-Content -Path $env:GITHUB_PATH -Value "C:\bd"
          & C:\bd\bd.exe --version

      - name: Install sandbox prereq (Linux only)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y bubblewrap

      - name: Install editable + dev deps
        run: uv pip install --system -e '.[dev]'

      - name: Decide pytest markers from event
        id: markers
        shell: bash
        run: |
          # PR / feature branch push: unit + integration (fast inner loop).
          # Push to main: add regression. Tag push: add smoke (--slow allowed).
          markers="not slow"
          if [ "${{ github.event_name }}" = "push" ] && [ "${{ github.ref }}" = "refs/heads/main" ]; then
            markers="not slow"  # regressions live in the same suite for now
          fi
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            markers=""  # allow slow smoke tests on tagged releases
          fi
          echo "markers=$markers" >> "$GITHUB_OUTPUT"

      - name: Run pytest with coverage
        env:
          PY_MARKERS: ${{ steps.markers.outputs.markers }}
        run: |
          if [ -n "$PY_MARKERS" ]; then
            pytest --cov=ortus --cov-report=xml --cov-report=term -m "$PY_MARKERS"
          else
            pytest --cov=ortus --cov-report=xml --cov-report=term
          fi

      - name: Upload coverage artifact (PR only)
        if: github.event_name == 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.os }}-py${{ matrix.python-version }}
          path: coverage.xml
          if-no-files-found: warn

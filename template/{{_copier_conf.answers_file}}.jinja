# Changes here will be overwritten by Copier; NEVER EDIT MANUALLY.
#
# This records the answers this project was generated with so `copier update`
# can re-render it, and so tooling can read the choices back — notably
# `agent_cli`, which tells tail.sh and the loops which backend the project was
# built for.
{{ _copier_answers | to_nice_yaml -}}
